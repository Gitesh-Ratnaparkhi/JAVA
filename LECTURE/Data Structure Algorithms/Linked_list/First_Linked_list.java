public class First_Linked_list {

    // creaing the node of the link list
    public static class node {
        int data;
        node next;
    }

    public static void main(String[] args) {
        node x = new node();
        System.out.println(x.next);
    }
}
// A linked list in Java is a dynamic data structure whose size increases as you
// add the elements and decreases as you remove the elements from the list. The
// elements in the linked list are stored in containers. The list holds the link
// to the first container.
